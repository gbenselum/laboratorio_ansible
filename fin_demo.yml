

# borra el usuario toxico en caso de existir
- hosts: Linux
  tasks:
  - name: elimina el usuario toxico
    user:
      name: toxico
      state: absent

- hosts: webservers
  tasks:
  - name: desinstala httpd
    package:
      name:httpd
      state:absent
